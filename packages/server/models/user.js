"use strict";
const { Model } = require("sequelize");
module.exports = (sequelize, DataTypes) => {

  //TODO : user 테이블과 연결되는 모델을 정의 하세요
  //공식 문서를 이용해 직접 정의하는 대신, 이 파일을 지우고, sequelize-cli를 이용해 다시 만들어도 좋습니다.
};

